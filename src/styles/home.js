import styled from '@emotion/styled';

const Container = styled.div`
  padding-bottom: 52px;
`;

const TxtFetchStatus = styled.p`
  text-align: center;
`;

export default {
  Container,
  TxtFetchStatus,
};
